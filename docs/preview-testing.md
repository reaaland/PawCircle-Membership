# Preview testing

Vercel Preview deployments require `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` to be assigned to the Preview environment.

For messaging changes, test the `fix/message-mobile-save-delete` branch preview before promoting or merging to production.
